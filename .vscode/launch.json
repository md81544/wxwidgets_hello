{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "hello",
            "program": "${workspaceFolder}/build/hello",
            "args": [],
            "cwd": "${workspaceFolder}"
        },

    ]
}